---
title: 'SigLens'
description: 'LLM Observability with SigLens and OpenLIT'
---

<Frame>
  <img src="/images/siglens-dashboard-1.png" />
  <img src="/images/siglens-dashboard-2.png" />
</Frame>

To directly send OpenTelemetry metrics and traces generated by OpenLIT from your AI Application to SigLens, Follow the below steps.
<Steps>
  <Step title="Add the following two lines to your application code:">
    <Tabs>
      <Tab title="Setup using function arguments">
      ```python
      import openlit

      openlit.init(
        otlp_endpoint="YOUR_SIGLENS_HTTP_ENDPOINT", 
      )
      ```

      Replace:
      1. `YOUR_SIGLENS_HTTP_ENDPOINT` with the Ingestion HTTP Endpoint value. Its generall of the format `http://<siglens-url>:4318/otlp`. 
        * Example - `http://localhost:4318/otlp`
      </Tab>
      <Tab title="Setup using Environment Variables">
      ```python
      import openlit

      openlit.init()
      ```

      Run the following command to configure the OTEL endpoint and headers to send metrics and traces to SigLens:
      ```shell
      export OTEL_EXPORTER_OTLP_ENDPOINT = "YOUR_SIGLENS_HTTP_ENDPOINT"
      ```

      Replace:
      1. `YOUR_SIGLENS_HTTP_ENDPOINT` with the Ingestion HTTP Endpoint value. Its generall of the format `http://<siglens-url>:4318/otlp`. 
        * Example - `http://localhost:4318/otlp`

      </Tab>
    </Tabs>
  Refer to the OpenLIT [Python SDK repository](https://github.com/openlit/openlit/tree/main/sdk/python) for more advanced configurations and use cases.
  </Step>
  <Step title="Visualise the traces">
  1. Log into your SigLens Instance.
  1. Click **Tracing** in the side navgivation menu.
  1. Click on any trace to see details.

  </Step>
</Steps>


---

<CardGroup cols={2}>
<Card title="Integrations" href="/latest/integrations/introduction" icon='circle-nodes'>
Start Monitoring your LLM Application 
</Card>
</CardGroup>
