# React 19 upgrade: some UI dependencies (e.g. @textea/json-viewer, which is
# built on React-19-compatible MUI 6) still declare their peer as react ^17||^18
# even though they work with 19. Relax strict peer resolution so `npm install`
# and the Docker build succeed. Remove this once those deps update their peers.
legacy-peer-deps=true
