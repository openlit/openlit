# Scoped Agent Guides

`AGENTS.md` is the repository-wide instruction set. These files hold the
canonical, area-specific guidance it directs an agent to read before changing
that area.

Cursor rules in `.cursor/rules/` are intentionally thin wrappers: they provide
Cursor metadata and reference these same guides. Keep implementation guidance
here rather than duplicating it in tool-specific files.
